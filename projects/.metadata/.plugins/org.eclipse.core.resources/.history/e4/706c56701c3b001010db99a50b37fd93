int sum_square(int n);

int main()
{
	printf ("1. is %i\r\n",sum_square(5));
	return 0;
}

int sum_square(int n)
{
	int i;
	int sum = 0;
	for(i = 1; i <= n; i++)
		sum+=i*i;
	return sum;
}

int** is_pyth()
{
	int** result = malloc(5*sizeof(int*));
	return 0;
}